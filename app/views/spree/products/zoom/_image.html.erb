<a href='<%= @product.images.empty? ? (@product.variant_images.empty? ? "javascript:void(0)" : @product.variant_images.first.attachment.url(:large)) : @product.images.first.attachment.url(:large) %>' title='<%= @product.name %>' rel='product-gallery' class='spree_jqzoom'>
  <%= render_original -%>
</a>
<div class='clearfix'></div>